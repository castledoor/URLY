
<div id='partial'>
  <h1 class="short_url"></h1>
  <div class="space">
  </div>

  <div class="shortTable">
    <p class="result"><a href="<%= request.base_url +  '/' + @url.shortened_url.to_s %>" target="_blank"><%= request.base_url + '/' + @url.shortened_url.to_s %></a></p>

    <p>This has been viewed <%= "#{@url.visits.count}" %> times.</p>
  </div>
</div>